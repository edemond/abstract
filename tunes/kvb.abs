let korg = instrument("UM-2 MIDI 1 (hw:1,0,0)", 1, 10)

let kick = note(36)
let snare = note(38)
let lotom = note(43)
let hitom = note(50)
let ch = note(42)
let oh = note(46)
let clap = note(39)
let crash = note(49)
let agogo = note(67)
let claves = note(75)

default korg oh

rhythm(0x8888, 0x8000, 0x8080, 0x8000)
